# Live web dashboard (browser UI: camera feed + detection overlays).
# Optional extra -- the core package and CLI scripts work without it.
# Install alongside the vision stack:  pip install -r requirements-vision.txt -r requirements-app.txt
flask>=3.0
